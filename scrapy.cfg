# Scrapy project configuration. Points `scrapy` CLI at our settings module so
# `scrapy crawl wrc` works from the project root in addition to `wrc-scrape`.
[settings]
default = wrc_pipeline.scraper.settings
